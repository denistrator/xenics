[package]
name = "xenics-stack-validation"
version = "0.1.0"
edition = "2021"

[dependencies]
markdown = "1.0.0"
rusqlite = { version = "0.40.2", features = ["bundled"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
